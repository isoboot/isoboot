---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: bootsources.isoboot.github.io
spec:
  group: isoboot.github.io
  names:
    kind: BootSource
    listKind: BootSourceList
    plural: bootsources
    shortNames:
    - bs
    singular: bootsource
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.message
      name: Message
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BootSource is the Schema for the bootsources API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of BootSource.
            properties:
              firmware:
                description: firmware is an optional firmware/driver archive to combine
                  with initrd.
                properties:
                  shasum:
                    description: shasum is the expected checksum (SHA256 or SHA512)
                      of the resource.
                    type: string
                  shasumURL:
                    description: shasumURL is the URL to a checksum file (e.g., SHA256SUMS
                      or SHA512SUMS) for verification.
                    type: string
                  url:
                    description: url is the download URL for the resource.
                    type: string
                required:
                - url
                type: object
                x-kubernetes-validations:
                - message: at least one of shasumURL or shasum must be specified
                  rule: has(self.shasumURL) || has(self.shasum)
                - message: shasumURL and shasum are mutually exclusive
                  rule: '!(has(self.shasumURL) && has(self.shasum))'
              initrd:
                description: initrd is a direct initrd download source.
                properties:
                  shasum:
                    description: shasum is the expected checksum (SHA256 or SHA512)
                      of the resource.
                    type: string
                  shasumURL:
                    description: shasumURL is the URL to a checksum file (e.g., SHA256SUMS
                      or SHA512SUMS) for verification.
                    type: string
                  url:
                    description: url is the download URL for the resource.
                    type: string
                required:
                - url
                type: object
                x-kubernetes-validations:
                - message: at least one of shasumURL or shasum must be specified
                  rule: has(self.shasumURL) || has(self.shasum)
                - message: shasumURL and shasum are mutually exclusive
                  rule: '!(has(self.shasumURL) && has(self.shasum))'
              iso:
                description: iso is an ISO image source containing kernel and initrd.
                properties:
                  initrdPath:
                    description: initrdPath is the path within the ISO to the initrd
                      file.
                    minLength: 1
                    type: string
                  kernelPath:
                    description: kernelPath is the path within the ISO to the kernel
                      file.
                    minLength: 1
                    type: string
                  shasum:
                    description: shasum is the expected checksum (SHA256 or SHA512)
                      of the resource.
                    type: string
                  shasumURL:
                    description: shasumURL is the URL to a checksum file (e.g., SHA256SUMS
                      or SHA512SUMS) for verification.
                    type: string
                  url:
                    description: url is the download URL for the resource.
                    type: string
                required:
                - initrdPath
                - kernelPath
                - url
                type: object
                x-kubernetes-validations:
                - message: at least one of shasumURL or shasum must be specified
                  rule: has(self.shasumURL) || has(self.shasum)
                - message: shasumURL and shasum are mutually exclusive
                  rule: '!(has(self.shasumURL) && has(self.shasum))'
              kernel:
                description: kernel is a direct kernel download source.
                properties:
                  shasum:
                    description: shasum is the expected checksum (SHA256 or SHA512)
                      of the resource.
                    type: string
                  shasumURL:
                    description: shasumURL is the URL to a checksum file (e.g., SHA256SUMS
                      or SHA512SUMS) for verification.
                    type: string
                  url:
                    description: url is the download URL for the resource.
                    type: string
                required:
                - url
                type: object
                x-kubernetes-validations:
                - message: at least one of shasumURL or shasum must be specified
                  rule: has(self.shasumURL) || has(self.shasum)
                - message: shasumURL and shasum are mutually exclusive
                  rule: '!(has(self.shasumURL) && has(self.shasum))'
            type: object
            x-kubernetes-validations:
            - message: must specify either (kernel and initrd) or iso
              rule: (has(self.kernel) && has(self.initrd)) || has(self.iso)
            - message: cannot specify both iso and kernel/initrd
              rule: '!(has(self.iso) && (has(self.kernel) || has(self.initrd)))'
          status:
            description: status defines the observed state of BootSource.
            properties:
              message:
                description: message provides human-readable details about the current
                  phase.
                type: string
              phase:
                description: phase is the current phase of the BootSource.
                type: string
              resources:
                additionalProperties:
                  description: ResourceStatus represents the status of an individual
                    downloaded resource.
                  properties:
                    path:
                      description: path is the local file path.
                      type: string
                    shasum:
                      description: shasum is the verified checksum of the resource.
                      type: string
                    size:
                      description: size is the file size in bytes.
                      format: int64
                      type: integer
                    url:
                      description: url is the URL the resource was downloaded from.
                      type: string
                  type: object
                description: |-
                  resources contains per-resource status information.
                  Keys include: kernel, initrd, iso, firmware, initrdWithFirmware.
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
